let INDICE = 13;
let SOMA = 0;
// Podendo ser feito com laço de repetição for, utilizando o While
for (let K = 1; K <= INDICE; K++) {

    SOMA = SOMA + K;
}

// Console no terminal (node soma.js) vai sair o resultado da Variavel Soma.
console.log(SOMA); // Saída: 91
